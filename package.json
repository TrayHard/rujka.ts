{
  "name": "rujka-discord-bot",
  "version": "1.0.0",
  "description": "Discord bot for RUJKA Discord server",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "start:local": "yarn build && node dist/index.js",
    "start:watch": "nodemon",
    "dev": "yarn && ts-node-dev --watch --respawn --transpile-only ./src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@types/node": "^14.14.10",
    "@types/pino": "^6.3.4",
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.6",
    "pino": "^6.7.0",
    "ts-node": "^9.1.0",
    "typescript": "^4.1.2"
  },
  "devDependencies": {
    "pino-pretty": "^4.3.0",
    "ts-node-dev": "^1.0.0"
  }
}
